global:
  scrape_interval: 5s
  evaluation_interval: 5s

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['127.0.0.1:9093']

rule_files:
  - 'alert_rules.yml'

scrape_configs:
  - job_name: 'snmp-trap-receiver'
    static_configs:
      - targets: ['127.0.0.1:9162']
