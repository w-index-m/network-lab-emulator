fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
websockets>=12.0
rank_bm25>=0.2.2
# gNMI(gRPC)サーバ用。未導入でも engine/gnmi_agent.py は
# _HAS_GRPC=False で無効化されるだけでアプリは起動する。
grpcio>=1.60
grpcio-tools>=1.60
# 実NETCONFサーバ(830)・実SSH CLIサーバ(22)が使う実SSH実装。
# 未導入でも各エージェントは _HAS_PARAMIKO=False で無効化されるだけで
# アプリは起動するが、ここに書かないとその機能が有効にならない
# （このリポジトリで実際に使っているのに requirements.txt に
# 無いことに気付かず放置されていた）。
paramiko>=3.4
